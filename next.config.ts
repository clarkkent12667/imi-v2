import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Note: middleware.ts is still valid in Next.js 16
  // The warning about "proxy" is informational - middleware.ts works correctly
};

export default nextConfig;
